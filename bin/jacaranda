#!/usr/bin/env ruby

require 'jacaranda'
req = Jacaranda::Request.new(
  :key => 'port',
  :namespace => [ 'apache' ],
  :policy => :craig,
  :lookup_type => :first,
  :metadata => { :environment => 'development' }
) 

jac = Jacaranda.new

answer = jac.lookup(req)
puts answer.payload
